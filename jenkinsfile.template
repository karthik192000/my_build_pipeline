pipeline {
    agent { 
        node {
            label 'docker_agent_alpine'
            }
      }
    triggers {
        pollSCM '*/5 * * * *'
    }  
    stages {
        stage('Build') {
            steps {
                echo "Building Now.."
                sh '''
                git clone https://github.com/karthik192000/Test.git
                cd Test
                javac Main.java
                '''
            }
        }
        stage('Test') {
            steps {
                echo "Testing.."
                sh '''
                ls -al
                java Main
                '''
            }
        }
        stage('Deliver') {
            steps {
                echo 'Deliver....'
                sh '''
                echo "doing delivery stuff.."
                '''
            }
        }
    }
}
