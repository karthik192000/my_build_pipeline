pipeline {
    agent { 
        node {
            label 'docker_agent_alpine'
            }
      }
    triggers {
        pollSCM '*/5 * * * *'
    }  
    stages {
        stage('Build') {
            steps {
                echo "Building Now.."
                sh '''
                javac Main.java
                '''
            }
        }
        stage('Test') {
            steps {
                echo "Testing.."
                sh '''
                java Main
                '''
            }
        }
        stage('Deliver') {
            steps {
                echo 'Deliver....'
                sh '''
                echo "doing delivery stuff.."
                '''
            }
        }
    }
}
